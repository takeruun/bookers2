<div class='container px-5 px-sm-0'>
  <div class='row'>
    <div class='col-md-12'>
        <h2>Follower Users</h2>
        <% if @users.count > 0 %>
          <%= render 'relationship_user', users: @users %>
        <% else %>
          ユーザはいません
        <% end %>
      </div>
  </div>
</div>